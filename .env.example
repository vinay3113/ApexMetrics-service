# Optional. When absent, ApexMetrics uses clearly labeled demo data.
FINNHUB_API_KEY=

# Public URL for metadata / canonical links

# Public production URL
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Stripe subscription billing (required before checkout is enabled)
STRIPE_SECRET_KEY=
STRIPE_PRICE_PRO_MONTHLY=
BILLING_SIGNING_SECRET=

# Optional public analytics ID/provider hook can be added later.

# Keep this label aligned with the Stripe Price configured above.
PRO_PRICE_DISPLAY=$19
